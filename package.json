{
	"name": "litrpg-chart",
	"private": true,
	"version": "1.0.0",
	"type": "module",
	"workspaces": ["packages/*"],
	"scripts": {
		"dev": "bun run --cwd packages/web dev",
		"build": "bun run --cwd packages/web build",
		"check": "bun run check:pipeline && bun run check:web",
		"check:pipeline": "bunx tsc --project packages/pipeline/tsconfig.json --noEmit && bunx tsc --project packages/contract/tsconfig.json --noEmit",
		"check:web": "bun run --cwd packages/web check",
		"test": "bunx vitest run --root packages/pipeline",
		"pipeline": "bun packages/pipeline/src/cli.ts"
	},
	"devDependencies": {
		"typescript": "^5.9.3",
		"vitest": "^4.1.2"
	}
}
